# Authn-Authz
 A .NET 10 ASP.NET Core Web API demonstrating JWT-based authentication and role/policy-based authorization. Includes token issuance on login, protected endpoints secured with [Authorize] (role and claim policies), and a token-decode endpoint to illustrate stateless JWT validation.
